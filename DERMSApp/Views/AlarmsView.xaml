<UserControl x:Class="DERMSApp.Views.AlarmsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DERMSApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="10,10">
            <DataGrid HorizontalAlignment="Left" VerticalAlignment="Top" ItemsSource="{Binding Alarms}" GridLinesVisibility="None" VerticalScrollBarVisibility="Auto" IsReadOnly="True" ColumnWidth="*"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Archive" Width="75"/>
                <Button Content="Clear log"  Width="75" Command="{Binding ClearAlarmsCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
